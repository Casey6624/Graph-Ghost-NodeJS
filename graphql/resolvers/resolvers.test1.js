/* const request = require("supertest");

describe("GraphQL API Tests", () => {
  let server;

  beforeEach(() => {
    server = require("../../app").default;
  });

  afterEach((done) => {
    server.close();
    done();
  });

  it("find a query in DB");
});
 */
